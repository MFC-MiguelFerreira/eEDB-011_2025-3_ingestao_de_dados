Requisitos:
- Python 3.10+ com `pip`
- Java 11+ (para Spark)
- Kafka e Postgres do docker-compose rodando
- `.env` configurado

Instalar deps:
```bash
pip install -r atividade8/consumer/requirements.txt